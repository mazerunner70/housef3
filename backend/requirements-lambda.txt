# Lambda Runtime Dependencies
# AWS SDK (boto3) is provided by the Lambda runtime

# Core dependencies
python-dateutil>=2.8.2
requests>=2.28.0
pydantic>=2.0.0 
python-jose>=3.3.0 

urllib3>=2.5.0 # not directly required, pinned by Snyk to avoid a vulnerability
